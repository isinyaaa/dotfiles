[user]
    name = isinyaaa
    email = isabellabdoamaral@usp.br
    signingKey = 217AB88EFC8BC1FF
[credential]
        # username = isinyaaa
        # helper = "!f() { test $1 = get && echo password=$(pass github/oauth); }; f"
	helper = /usr/bin/pass-git-helper
        # helper = cache --timeout=3600
[init]
	defaultBranch = main
[commit]
	gpgSign = true
[color]
	ui = auto
[core]
	autocrlf = input
[diff]
        tool = vscodium
[difftool "vscode"]
        cmd = vscodium --wait --diff $LOCAL $REMOTE
[merge]
        tool = vscodium
[mergetool "vscode"]
        cmd = vscodium --wait $MERGED
