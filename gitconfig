[user]
    name = Isabella Basso
    email = isabbasso@riseup.net
    signingKey = E0EA61F34ADB15C4
[sendemail]
    confirm = auto
    smtpEncryption = ssl
    smtpServer = mail.riseup.net
    smtpServerPort = 465
    smtpUser = isabbasso
[includeIf "gitdir:~/repos/usp"]
    path = ~/.gitconfig-usp
[credential]
    helper = /usr/bin/pass-git-helper
[init]
    defaultBranch = main
[format]
    signOff = true
[commit]
    gpgSign = true
[color]
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    pager = true
    ui = auto
[core]
    autocrlf = input
    pager = diff-so-fancy | less -RFX
[diff]
    tool = nvim
    submodule = log
[difftool]
    prompt = true
[difftool "nvim"]
    cmd = nvim -d \"$LOCAL\" \"$REMOTE\"
[difftool "vscodium"]
    cmd = vscodium --wait --diff \"$LOCAL\" \"$REMOTE\"
[merge]
    tool = nvim
[mergetool]
    prompt = false
[mergetool "nvim"]
    cmd = nvim -f -c \"Gdiffsplit!\" \"$MERGED\"
[mergetool "vscodium"]
    cmd = vscodium --wait \"$MERGED\"
